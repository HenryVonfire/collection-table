<h1>Simple ember-collection Twiddle Demo!</h1>
<div style=>
  {{head-tab columns=columns class="head"}}
</div>

<div class="collection">
  {{#ember-collection items=model cell-layout=(fixed-grid-layout 600 41) as |item|}}
    {{comp-tab item=item columns=columns}}
  {{/ember-collection}}
</div>
